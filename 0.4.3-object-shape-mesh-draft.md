
## draft

for shaders that are easy compatible with any geometry implement selector of object mesh or analytic formula. skip shaders that not support or hard implement such.

make plans

### 0.4.3-select-shape-plan.md
buttons that make all objects (currently spheres) become:
sphere
cube
tetrahedron
knout (typical 3d demo object)

### 0.4.4-heightmap-scene-plan.md
new scene:

heightmap (simplex) constrained top of cube (all using main selected shader). + other mesh that represent lake (better use blue tint glass). looks like piece of landscape made from glass.

heightmap (simplex) constrained top of cube (using solid color height-colorized shader)  + other mesh that represent lake (main selected shader). looks like piece of opaque landscape + glass-water.

